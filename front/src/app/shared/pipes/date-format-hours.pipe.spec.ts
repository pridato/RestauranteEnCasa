import { DateFormatHoursPipe } from './date-format-hours.pipe';

describe('DateFormatHoursPipe', () => {
  it('create an instance', () => {
    const pipe = new DateFormatHoursPipe();
    expect(pipe).toBeTruthy();
  });
});
